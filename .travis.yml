sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t youn7054/docker-react -f Dockerfile.dev .

script:
    - docker run -r CI=true youn7054/docker-react npm run test  --  --coverage

after_success:
  - echo "test successabbb"